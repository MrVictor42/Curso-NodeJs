<div class="card">
    <div class="card-body">
        <h1> {{post.title}} </h1>
        <hr>
        <small> CreatedAt: {{post.createdAt}} </small><br>
        <hr>
        {{post.content}}
    </div>
</div>